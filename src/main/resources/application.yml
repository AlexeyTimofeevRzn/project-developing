spring:
  application:
    name: project-developing
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

datasource:
  url: jdbc:postgresql://localhost:5432/project-developing-dev
  user: postgres
